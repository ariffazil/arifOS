/**
 * @arifos/kernel — Shared Type Contracts
 *
 * This module provides the canonical TypeScript type definitions
 * shared between the Python kernel (judge) and the TS A-FORGE executor.
 *
 * The actual kernel logic lives in Python at:
 *   /root/arifOS/arifosmcp/runtime/kernel/
 *
 * A-FORGE executor logic lives in TS at:
 *   /root/A-FORGE/src/executor/
 *
 * DITEMPA BUKAN DIBERI — Forged, Not Given.
 */

export * from "./types.js";
