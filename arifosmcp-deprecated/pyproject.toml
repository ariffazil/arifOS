[project]
name = "arifosmcp"
version = "2026.07.17.post1"
description = "DEPRECATED — arifosmcp has been renamed to arifos. Use 'pip install arifos' instead."
requires-python = ">=3.12"
dependencies = ["arifos>=1!2026.7.17.post1"]
[build-system]
requires = ["setuptools>=82"]
build-backend = "setuptools.build_meta"
[tool.setuptools.packages.find]
where = ["src"]
